import styled from "styled-components";


// Pay attention, styled containers names must start with CAPITAL letters. "dummyContainer" would fail.

export const DummyContainer = styled.div`
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    height: 10px;    
    width: 10px;    
`